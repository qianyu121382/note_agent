## 一、项目核心定位
专为Agent 岗位面试打造的实战型 LangGraph 项目，核心解决：多源文件 / 链接自动解析 + 个性化无重复笔记生成 + AI 配图一体化需求，完整体现 LLM Agent、工作流编排、工具调用、本地文件交互等核心能力。

## 二、核心需求
*   **输入源**:
    *   **构思**: 支持用户上传网页链接、视频、文档等多种形式，支持多个文件 / 链接同时输入。
    *   **当前实现状态**: ✅ 已实现**网页链接**和**大段文本**作为输入源。可在一个输入中处理多个链接或文本片段。
*   **核心处理**:
    *   **构思**: LLM 对多源内容进行解析、整理、归纳。
    *   **当前实现状态**: ✅ 已使用 LLM 实现**输入意图识别**和**数据提取**（`dispatcher` 节点）。笔记生成部分目前为**占位符**，未调用 LLM。
*   **关键能力**:
    *   **构思**: 自动读取用户本地笔记，识别已学习内容，不重复生成笔记。
    *   **当前实现状态**: ❌ **未实现**。
*   **输出要求**:
    *   **构思**: 生成Markdown 格式笔记。
    *   **当前实现状态**: ✅ 已实现基础的文本笔记输出。
*   **个性化配置**:
    *   **构思**: 支持用户提前设定笔记格式、上传示例等。
    *   **当前实现状态**: ❌ **未实现**。
*   **扩展能力**:
    *   **构思**: LLM 可自主调用图片生成模型，为笔记指定部分生成配套图片。
    *   **当前实现状态**: ❌ **未实现**。

## 三、项目技术栈核心（面试导向，仅列核心）
*   **工作流编排**: LangGraph（核心框架，体现 Agent 状态管理、流程控制）
*   **大模型能力**: LLM（内容解析 / 格式控制 / 决策） + 文生图模型（AI 配图）
*   **基础能力**: 多源文件解析、本地文件读写、Markdown 格式化输出

## 四、核心工作流（已实现基础框架）
1.  **输入与意图分析 (Input & Dispatch)**: **[✅ 已实现]** 流程的入口，基于 LLM 分析用户输入，识别是进行笔记 (`note_taking`)、闲聊 (`waiting`) 还是退出 (`exit`)，并提取出 URL 或文本。
2.  **内容提取 (Content Ingestion)**: **[✅ 已实现]**
    *   将 URL 和文本分离。
    *   通过 `fetch` 工具异步抓取所有 URL 的网页内容。
    *   将抓取到的内容和用户直接输入的文本内容整合为统一的原始文本流。
3.  **核心内容生成 (Core Content Generation)**: **[🟡 部分实现 - 占位符]**
    *   当前为**占位符**实现，仅将提取到的原始内容截取前 500 个字符作为笔记摘要。
    *   **下一步**: 将此节点替换为真正的 LLM 调用，对原始内容进行归纳、总结，生成“内容核心稿”。
4.  **初步结构化 (Initial Structuring)**: **[❌ 未实现]**
5.  **内容去重 (Content Deduplication)**: **[❌ 未实现]**
6.  **个性化与输出 (Personalization & Output)**: **[❌ 未实现]**

## 五、面试亮点提炼（基于你的需求）
*   多模态 / 多源数据处理能力
*   本地文件交互 + 上下文记忆（无重复笔记）
*   个性化格式适配（示例学习 + 规则配置）
*   LLM 自主工具调用（文生图模型）
*   LangGraph 完整工作流编排（Agent 核心能力）

## 六、当前阶段
**阶段一完成：核心框架搭建与内容提取**

项目已从纯构思阶段进入实际开发阶段，并完成了第一阶段的核心任务。

*   **已完成**:
    *   **项目结构重构**: 按照功能子图（Dispatcher, Ingestion, Notes Generator）对代码进行了模块化重构，结构清晰，易于扩展。
    *   **核心工作流搭建**: 使用 LangGraph 搭建了完整的主工作流和子工作流（`dispatcher` -> `ingestion` -> `notes_generator`）。
    *   **意图分派**: 实现了基于 LLM 的 `dispatcher` 节点，可以准确分析用户意图并提取数据。
    *   **多源内容提取**: 实现了 `ingestion` 子图，可以并行处理 URL 和文本输入，并将其整合。
*   **下一步计划**:
    *   实现**核心内容生成**：将 `notes_generator` 中的占位符替换为真实的 LLM 总结功能。
    *   实现**内容去重**：引入向量数据库，实现与历史笔记的比对。
    *   实现**初步结构化**：在内容提取后，增加一个 LLM 节点，将杂乱的原文整理成结构化的 Markdown。
