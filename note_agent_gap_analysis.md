# Note Agent 面试导向缺口与优先级

这份清单按三个标准排序：

- 面试展示价值是否足够直观
- 实现成本是否可控
- 做出来后的效果是否稳定、容易讲清楚

不满足这三个标准的功能，先不放进主清单。

## 第一梯队：最值得继续做
- [ ] 更细粒度的多轮对话意图识别
  - 当前已经有 `create / edit / qa / idle` 的最小语境识别
  - 下一步最值得补的是：`edit` 子意图、`qa` 子意图、澄清响应识别、目标笔记切换识别
  - 这是对当前短期记忆能力最自然的延伸，面试里也很好讲
- [ ] 测试体系
  - 尤其是围绕多轮对话、状态流转、session restore、follow-up 的测试
  - 这项很加分，因为它能证明你的 agent 不是“看起来能跑”，而是有可验证行为
- [ ] RAG 完整知识库链路 + 引用与可追溯性
  - 这两项建议合并做，不要拆开
  - 如果只做 RAG 不做引用，展示效果一般；如果只讲引用没有检索链路，也不完整
  - 做好后非常适合面试展示，因为它同时覆盖检索、生成、可信度和工程链路
- [ ] 会话状态与笔记状态正式拆分
  - 这项已经做了一部分，但还没完全成体系
  - 建议作为“把系统设计讲扎实”的工程项来做，而不是再加很多零散字段
  - 如果时间有限，这项优先级略低于上面三项，但仍然值得保留

## 第二梯队：时间够再做
- [ ] 工程鲁棒性与错误处理
  - 这项有价值，但展示效果不如上面几项直接
  - 适合作为收尾提升，而不是优先主目标
- [ ] 前端产品化
  - 如果只是做简单 UI 美化，面试价值一般
  - 只有在你能明确展示“状态可视化 / 笔记流转 / 历史与当前模式联动”时才值得继续投入
- [ ] 长期记忆系统
  - 当前不算最优先
  - 如果后面要做，建议只做“跨线程用户稳定偏好”，不要一开始做复杂长期知识记忆

## 当前不建议继续投入的项
这些要么成本偏高、要么效果不稳定、要么面试亮点不足，当前版本建议先删掉，不再作为主目标：

- [x] 长上下文处理
- [x] 输入摄入能力增强
- [x] `skills` 的专门上下文
- [x] `skills` 的专门资源文件
- [x] `skills` 的专门执行约束与边界
- [x] `skills` 的更细粒度工具隔离
- [x] 持久化策略升级
- [x] 基于已有笔记的二次操作能力
  - 当前已经有最小能力，不值得继续单列成高优先级目标

## 已经完成的、值得在面试中讲的基础能力
- [x] 笔记对象模型与元数据管理的最小版
- [x] 基于 `note_id` 的笔记 CRUD 基础能力
- [x] 修改笔记主链路的最小可用版
- [x] 会话持久化与历史记录恢复
- [x] 前端历史记录展示
- [x] 最小版 `skills` 机制
  - 已支持 `create_note`、`edit_note`、`note_qa`
  - 已支持 agent 根据任务先加载对应说明再执行
- [x] 最小版结构化短期记忆
  - 已正式引入 `mode` 作为线程级会话控制状态
  - 已保留 `active_note_id` / `active_note_title` 作为当前对象状态
  - 已实现 session restore -> dispatch -> react -> save 的统一流转
  - 已在前端线程历史中展示当前 `mode`

## 当前最适合的推进顺序
1. 更细粒度的多轮对话意图识别
2. 测试体系
3. RAG 完整知识库链路 + 引用与可追溯性
4. 会话状态与笔记状态正式拆分
5. 工程鲁棒性与错误处理
6. 前端产品化
7. 长期记忆系统
